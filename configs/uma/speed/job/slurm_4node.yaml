# SLURM 32 GPU (4 nodes) job configuration
run_dir: /checkpoint/ocp/rgao/speed
run_name: test_speed_32gpu
scheduler:
  mode: SLURM
  num_nodes: 4
  ranks_per_node: 8
  slurm:
    account: ocp
    qos: h200_dev
    mem_gb: 0
    cpus_per_task: 24
graph_parallel_group_size: 32
